<!-- Keep it short. Delete sections that don't apply. -->

## What & why
<!-- One or two sentences: what changes and the reason. -->

## How verified (by execution)
- [ ] `node --test` is green
- [ ] `node bin/cli.mjs check --root examples` reports *in sync*
- [ ] Docs updated in this PR (`README.md` / `SPEC.md` / `CHANGELOG.md`) where behavior changed

## Notes
<!-- For load-bearing changes (engine / lifecycle / CLI / hooks): how you confirmed it doesn't
     corrupt or mis-generate a user's file. For new behavior: which test covers it. -->
